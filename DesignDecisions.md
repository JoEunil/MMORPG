# MMORPG 서버 설계 결정

MMORPG 서버가 세션 기반 게임(FPS/MOBA)과 갈라지는 지점을 정리한다. 
구현 상세는 각 기술 문서에 있고, 여기서는 왜 그 구조를 골랐는지만 다룬다.  

## 0. 출발점

한 유저가 매 틱 수백 명의 상태를 수신한다. 세션 기반은 이 값이 5~32다.  
아래 결정은 전부 이 자릿수 차이에서 나온다.  

| | 세션 기반 (FPS/MOBA) | MMORPG |
|---|---|---|
| 동시 엔티티 | 매치당 5~32 | Zone당 수백~수천 |
| 상태 수명 | 매치 종료 시 폐기 | 영구 지속, DB와 계속 정합 |
| 세션 지속 단위 | 매치 단위 | 	로그인~로그아웃 |
| 틱 | 60~128Hz | 20Hz |
| 정합 시점 | 매치 종료 후 1회 저장 | 가동 중 상시 정합 |
| 확장 방향 | 매치 인스턴스 추가 | 월드 분할 |

## 1. 상태 전파

### 1.1 직렬화를 오브젝트에 분산할 것인가, 중앙 루프에 모을 것인가

상용 엔진의 표준은 오브젝트 단위 리플리케이션이다. 네트워크 오브젝트가 dirty flag를 들고 스스로 직렬화하며, relevance 시스템이 커넥션별 전송 여부를 판단한다.  
- 이 방식은 작업 단위가 (오브젝트 × 커넥션)이 된다. 커넥션마다 자기 relevance 집합과 baseline을 들고 페이로드를 따로 조립하기 때문이다. 32명 규모에서는 문제가 안 되지만, AOI 안에 수백 명이 들어오면 이 곱셈이 틱 예산을 먹는다.  

그래서 조립 단위를 커넥션이 아니라 Cell로 잡았다. 같은 Cell을 보는 세션은 완전히 같은 바이트를 받으므로 Cell당 한 번만 만들어 공유한다. 대신 커넥션별 baseline을 포기해야 한다. 모든 수신자가 같은 것을 봐야 성립하는 구조라, 놓친 상태는 주기적인 Full Snapshot으로 덮는다.  
- 대가는 확장성이다. 필드를 추가하면 struct와 직렬화 코드를 함께 고쳐야 하고, 커넥션별 맞춤 전송은 불가능하다.  

→ [Snapshot](Snapshot.md), [AOI](AOI.md)

### 1.2 대역폭

전송량은 접속자 수에 선형으로 붙지 않는다. 보내는 쪽도 받는 쪽도 인원 수라 제곱에 가깝게 늘어난다. AOI는 그 제곱을 Zone 전체가 아니라 Cell 단위로 잘라내는 장치다.

비용은 세 항으로 분해되고, 각각을 따로 줄였다.

- **대상 수** — Cell 기반 AOI로 인접 9칸만 전파
- **페이로드** — Full / Delta 분리
- **패킷 수** — 여러 Cell의 조각을 Chunk로 병합해 일괄 송신

## 2. 시뮬레이션

### 2.1 방이 아니라 Zone

세션 기반의 방은 인원이 고정되고 수명이 매치 단위다. 서버 인스턴스 하나가 방 하나를 가지면 되니 분할 경계가 명확하다.

Zone은 상시 존재하고, 인원이 유동적이고, 유저가 Zone 사이를 오간다. 그래서 분할 단위가 매치가 아니라 공간이 되고, Zone마다 전용 스레드가 틱 루프를 돈다.
의도한 것은 Zone 안에서 lock을 없애는 것인데 아직 절반이다. 틱 로직은 Zone 스레드가 단독으로 갖지만, 진입·이탈·프로필 갱신은 외부 스레드가 `ZoneState`에 직접 들어와 mutex로 직렬화한다. Zone에 이벤트 큐를 붙이면 이 경로도 틱 루프 안으로 들어온다. 그 전에 Zone이 다른 Zone의 연결을 어떤 구조로 참조할지 정해야 한다

### 2.2 20Hz Tick

틱 예산은 틱 간격 × 코어 하나로 고정이고, 그 예산을 엔티티 수가 쓴다. 수백~수천 명 규모에서는 예산이 인원 수로 소진되므로 틱을 낮춰 수용 인원을 산다.

- 낮춘 만큼은 클라이언트가 메운다. 내 캐릭터는 예측으로, 원격 엔티티는 스무딩으로 처리한다. 스냅샷 두 개를 버퍼링해 그 사이를 렌더하는 교과서적 보간은 쓰지 않았다. 궤적은 정확해지지만 원격 월드를 50~100ms 과거로 그리기 때문이다. 지금 방식은 그 지연이 없는 대신 이동 중 위치가 뒤처진다.
- 어느 쪽이든 회복되는 것은 부드러움이지 정확도가 아니다. 원격 위치가 서버 좌표보다 뒤처진다는 사실은 남고, 그게 2.3 되감기의 전제다.
- 20Hz는 리플리케이션 주기이고, 게임 로직 틱이 이에 묶일 필요는 없다. 판정 해상도가 필요해지면 로직 틱만 올리고 리플리케이션은 20Hz에 두면 된다.

→ [Tick](Tick.md)

### 2.3 되감기(lag compensation) — 미적용

되감기는 쏜 시점에 클라이언트가 본 화면으로 서버가 과거를 되감아 명중을 판정한다. FPS에서 필수인 이유는 히트스캔이 순간 좌표 하나로 승패를 가르기 때문이다.

MMORPG는 스킬 판정이 범위·지속시간 기반이라 수십~수백 ms 오차를 흡수한다. 되감기가 주는 이득보다 판정이 허용하는 오차가 크므로 도입하지 않았다.

### 2.4 엔티티를 액터로 만들지 않은 이유

Zone 안에서는 엔티티 간 상호작용이 매 틱 일어난다. 액터로 쪼개면 상호작용 한 번마다 메시지 왕복이 붙고, 그 비용은 상호작용 밀도에 비례한다. 수백 명이 서로를 참조하는 틱에서는 메시지 오버헤드가 시뮬레이션 비용을 넘는다.
액터가 유효하려면 세 가지가 같이 와야 한다.

- 상태 독점
- 낮은 상호작용 빈도
- 짧은 수명

엔티티는 첫 번째만 만족한다. 몬스터 AI를 객체에 메서드로 붙이는 것도 액터화가 아니다. 상태를 하나의 실행 흐름만 건드린다는 속성은 Zone이 제공하고 있고, 메서드를 붙이는 것은 코드 조직 문제다. 동시성 단위는 Zone, 조직 단위는 상태머신으로 나눠서 본다.

> 액터 여부는 스케줄링 방식이 아니라 상태 캡슐화로 결정된다. 스레드풀·코루틴·파이버 중 무엇을 쓰든 그 상태를 한 번에 하나의 실행 흐름만 건드리면 액터다. 워커 여럿이 같은 상태에 lock을 걸고 들어가면 공유 상태 + mutex다.

### 2.5 던전 인스턴스 — 액터 후보 (미구현)

던전은 2.4의 세 조건을 다 만족한다.

- 외부 월드와 상태를 공유하지 않는다
- 엔티티 밀도가 낮다
- 파티 단위로 생겼다 사라진다

던전 하나가 액터 하나로 성립한다.

## 3. 트래픽

### 3.1 UDP가 아니라 TCP

FPS가 UDP를 쓰는 이유는 갱신 빈도다. 60Hz로 위치가 갱신되면 유실분을 재전송으로 기다리느니 다음 패킷을 받는 게 낫다. TCP는 순서 보장과 재전송이 묶여 있어서 유실 한 번이 뒤따라온 정상 패킷까지 붙잡는다.

MMO는 전제가 두 가지 다르다.

- 한 커넥션에 거래·인벤토리처럼 유실되면 안 되는 트래픽과 위치처럼 유실돼도 되는 트래픽이 섞여 있고, 앞쪽 비중이 크다. UDP로 가면 그 부분에 재전송·순서·흐름 제어를 직접 붙여야 하는데 그건 TCP를 다시 만드는 일이다.
- 범위·지속 기반 판정이라 재전송으로 생기는 stall을 게임플레이 허용치 안에서 견딜 수 있다.

대가는 스트림 경계 처리다. 메시지 단위가 보존되지 않으니 수신 측에서 누적 버퍼로 재조립해야 한다. 또 TCP는 상대가 조용히 사라져도 알려주지 않는다. 연결이 시간 단위로 유지되는 MMO에서는 half-open 세션을 능동으로 탐지해 끊어야 한다.

### 3.2 Producer-Consumer

P-C가 강제되는 이유는 트래픽 양이 아니다. 초당 패킷 수만 보면 FPS가 더 많다. 이유는 소유권이다. IOCP 완료 통지는 임의의 워커 스레드에서 올라오는데 Zone 상태는 하나의 실행 흐름이 가져야 한다. 수천 커넥션을 소수 워커로 받는 이상 수신 스레드와 로직 스레드는 일치할 수 없으니, 큐를 두고 실행 흐름을 갈아탄다.

### 3.3 종료 순서

P-C로 만든 대가가 종료에서 나온다. 각 경계에서 상류를 먼저 끊어야 하류가 남은 것만 처리하고 끝낼 수 있다. 순서가 뒤집히면 멈춘 큐로 계속 밀려들어 유실되거나, 입력이 끊기지 않아 drain이 안 끝난다.

MMO에서 이게 중요한 이유는 종료 시점에 메모리에만 있는 캐릭터 상태가 수천 명 분 남아 있기 때문이다. 세션형은 신규 배정을 끊고 기다리면 매치가 끝나면서 상태가 사라진다. MMO는 능동적으로 내려야 한다.

→ [Graceful Shutdown](GracefulShutdown.md)

## 4. 고정 객체풀

풀 크기는 설계 시점에 정하고 런타임에 늘리지 않는다. 고갈되면 Drop하고 로그를 남긴다. 동적 확장은 병목 시점에 추가 할당을 유발해서 지연을 메모리 쪽으로 옮길 뿐이다.

세션형과 다른 점이 두 가지 있다.

- 세션형은 매치가 끝나면 인스턴스를 버리고 부하가 늘면 인스턴스를 추가한다. MMO는 한 프로세스에 수천 명이 얹히므로 유저당 footprint × 동시접속 수가 곧 서버 스펙이 된다.
- 점검 사이 수일간 연속으로 도니, 유저가 들고나며 반복하는 할당·해제가 단편화로 쌓인다.

→ [ObjectPool](ObjectPool.md)

## 5. 영속성은 런타임 요구사항이다

세션형에서 durability는 매치가 끝난 뒤의 문제다. 진행 중에는 메모리 상태만 맞으면 된다.

MMO는 가동 중에도 인벤토리·골드·거래가 영속 계층과 맞아야 한다. 유저에게 아이템은 매치가 끝나는 시점이 아니라 획득한 순간부터 자산이기 때문이다.
- 접근 빈도가 높은 데이터는 매번 DB를 칠 수 없으니 Write-Back 캐시를 쓴다.
- Write-Back은 flush 전에 죽으면 dirty가 날아가니 복구 경로가 필요하다. 
- 거래는 캐시-DB 경계와 DB 간 경계를 동시에 넘으므로 Saga와 Outbox/Inbox로 처리한다. 
- 데이터 중요도에 따라 durability를 다르게 준다.

→ [CacheLib](CacheLib.md), [WAL](WAL.md), [Bazaar](Bazaar.md), [DB](DB.md)
