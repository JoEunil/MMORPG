# 유저 이름 처리 (Profile 분리)

## 1. 개요
이 문서는 캐릭터 이름을 Zone 상태에서 분리해 별도 Profile로 관리하도록 변경한 과정을 정리한 기술 문서이다.

## 2. 문제 상황
- Full Snapshot이 캐릭터 이름을 매번 그대로 실어 보내고 있었다.
	- 전체 스냅샷 필드 중 절반가량이 이름이었다. 이름은 거의 바뀌지 않는 필드인데, 1초에 한 번 전체 유저에게 다시 보내는 셈이라 낭비가 컸다.
- Chat 패킷도 메시지마다 발신자 이름을 실었다.
- 이름이 Zone 상태와 Chat 상태 두 군데에 각각 사본으로 존재해서, 이름을 바꾸면 두 지점을 모두 고쳐야 했다.
- 다른 zone·다른 채널에 있는 유저, 접속하지 않은 유저의 정보를 조회할 경로가 없었다. 이름이 Zone 상태에 묶여 있으면 애초에 그런 조회가 불가능하다.

## 3. 해결 방안
__DB 스키마 분리__  
캐릭터 이름을 별도 Profile 엔티티로 분리했다. 캐릭터는 이름 대신 Profile을 가리키는 키만 가진다. 
- Profile은 버전 값을 함께 가진다. 클라이언트가 캐싱한 이름이 낡았는지 판단하는 무효화 신호로 쓴다.
- Profile 키는 `char_id`와 별개의 키로 뒀다. PK를 외부에 노출하지 않는다는 원칙을 유지하고, 나중에 Profile을 계정 단위로 옮길 여지도 남긴다. 대신 캐릭터 생성 시 Profile 먼저 만들고 캐릭터를 만드는 순서와 트랜잭션이 필요해졌다.

__별도 캐시__  
기존 인벤토리/재화 캐시와 요구사항이 달라 별도로 만들었다.

| | 인벤토리 / 재화 | Profile |
|---|---|---|
| 쓰기 빈도 | 높음 | 거의 없음 |
| 정책 | write-back | write-through |
| eviction | LRU (flush 주기와 맞물림) | 불필요 |
| 상태 관리 | 4-state (조회 중 등) | 불필요 |

재화/인벤토리는 쓰기가 많아 write-back이 필요했고, 그 flush 주기에 맞춰 LRU eviction이 따라붙었다.   
Profile은 변경 빈도가 매우 낮아 write-through로 충분하고, write-through 구조에서는 캐시가 항상 DB와 일치하므로 eviction 정책 자체가 필요 없다.  

- 적재/삭제는 접속·종료 시점에 맞춘다.
- 읽기는 캐시에 있는 값만 반환하고, 없는 값은 DB를 뒤지지 않고 그냥 무시한다. DB 조회 과정이 아예 없으므로 캐시 상태를 보호할 필요도 없고, 그 덕분에 읽기를 큐 없이 동기로 처리할 수 있다.
- 쓰기(이름 변경)는 기존 비동기 큐 경로로 처리한다. 게임 코어 워커가 DB를 동기로 기다리는 건 적절치 않다고 판단했다.

__스냅샷 / 채팅 전파__
- Full Snapshot: 이름 필드를 빼고 Profile 키 + 버전만 싣는다.
- Delta Snapshot: 버전이 바뀌면 그 변경분만 델타로 전파한다.
- Chat: 메시지마다 이름 대신 Profile 키만 싣는다. 채팅 세션도 이름 대신 Profile 키만 들고 있어, 이름이 바뀌었을 때 갱신할 지점이 하나로 줄었다.

__클라이언트 조회__  
클라이언트는 Profile 키 → 이름 매핑을 캐싱하고, 모르는 키만 배치로 묶어 요청한다(한 번에 조회 가능한 개수는 제한).   
응답에는 서버 캐시에 있는 것만 담는다. 요청했는데 응답에 없는 키는 미접속 상태라 서버 메모리에 없다는 뜻이므로, 클라이언트는 반복 재요청하지 않아야 한다.   
오프라인 유저 정보 조회처럼 이 캐시로 안 되는 경우는 DB를 직접 조회하는 별도 경로를 쓴다.  

## 4. rename의 버전 역전파
버전을 올리는 주체는 캐시 레이어인데, 버전을 다른 클라이언트에게 **전파하는** 주체는 Zone 상태다. 캐시에서 버전을 올려도 Zone은 그 사실을 모르므로, 이름이 바뀐 게 같은 구역의 다른 클라이언트에게 영원히 전파되지 않는다.
그래서 이름 변경이 DB에 확정된 뒤, 그 결과를 다시 게임 코어 쪽으로 돌려보내 Zone의 캐릭터 상태에 반영하고 dirty 표시를 하는 왕복 경로를 뒀다.  

__Zone 갱신에 기존 락을 쓴 이유 (한계)__  
원래는 Zone에 이벤트 큐를 붙여서 이런 외부 요청과 Zone 간 이동을 함께 처리하려 했다. 그런데 Zone이 다른 Zone을 어떻게 참조할지 구상이 서지 않아(Zone 이동은 출발/도착 Zone 두 곳을 건드려야 한다), 일단 Zone에 이미 있는 락 구조를 그대로 썼다.
당장은 문제가 없다고 판단한 근거는, 이름 변경 빈도가 극단적으로 낮아 Zone 틱에 주는 영향이 사실상 없고, 캐릭터 입장/퇴장 처리가 이미 같은 락을 잡고 있어 새로운 경합 지점이 생기는 것도 아니라는 점이다.
추후 과제로, Zone에 이벤트 큐를 도입하면 Zone 간 이동과 외부 상태 갱신을 모두 큐로 직렬화할 수 있다. 이때 Zone 상호 참조 방식을 먼저 정해야 한다.

## 5. 검증
- 클라이언트는 이번 변경을 반영하지 않는다 — 수정할 부분이 너무 많다. 그래서 와이어 레이아웃이 바뀐 기존 클라이언트/부하테스트 도구는 이 변경 이후 동작하지 않는다.
- 검증은 캐시 레이어를 통한 통합테스트로 수행한다. (Google Test를 사용하기 위해서 UnitTests 프로젝트에 생성) 새로운 연결이 생겼을 때 로컬 캐시가 어떻게 반응하는지를 기준으로 확인한다.

## 6. 참고
- [ProfileCache](CacheLib/ProfileCache.h)
- [ProfileCache 통합테스트](UnitTests/CacheLib/ProfileCacheTest.cpp)
- [Snapshot 설계 문서](Snapshot.md)
