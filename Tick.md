# Tick 처리 

## 1. 개요
게임 로직에서 Tick은 시간 단위로 게임 상태를 갱신하고 클라이언트에 동기화하기 위한 기준점을 의미한다.  
틱 단위로 서버는 캐릭터 상태, 스킬, 몬스터 행동, Snapshot 전송 등을 처리하여 게임 로직의 일관성을 유지한다.

## 2. 목적
Tick 처리의 주요 목적은 다음과 같다.
1. 게임 로직 일관성 유지
- 스킬 쿨다운, DPS, 몬스터 AI, 이벤트 처리 등 시간 기반 계산을 정밀하게 수행
2. Snapshot 전송 주기 보정
- Delta / Full Snapshot 주기를 Tick 기준으로 관리하여 클라이언트와의 상태 동기화를 일정하게 유지
3. 서버 부하 조절 및 Tick 보정
- Tick 지연을 보정하여 일정한 주기(GAME_TICK)로 로직 처리
- CPU burst 방지

## 3. Tick 처리 흐름
Zone 스레드는 Tick 단위로 다음과 같은 작업을 수행한다:
1. Skill Cooldown 처리  
	- Zone 내 모든 캐릭터의 스킬 쿨다운을 갱신
	- 활성 스킬의 phase 틱 갱신
2. 패킷 처리  
	- Zone의 Work Queue에서 패킷을 처리
	- 각 패킷에 대해 handler->Process() 호출 → 게임 상태 갱신
3. 게임 상태 업데이트
	- handler->ApplySkill(zoneID) : 스킬 적용
	- handler->UpdateMonster(zoneID) : 몬스터 AI 처리
	- handler->FlushCheat(zoneID) : 치트 상태 업데이트
4. Snapshot 전송
	- Full Snapshot: FULL_SNAPSHOT_TICK 주기
	- Delta Snapshot: DELTA_SNAPSHOT_TICK 주기
	- 시간 계산 후 적절한 Snapshot 전송
5. Tick 지연 보정
	- 현재 시간과 마지막 Tick 시간 차이를 계산
	- 남은 시간 만큼 sleep_for 수행
	- lastTick += GAME_TICK 으로 Tick 기준 보정

> Windows 타이머 해상도는 약 15.6ms이므로, 그보다 작은 지연은 sleep_for 호출이 의미가 없고,  
실제 보정은 16ms 이상 지연 발생 시만 수행된다.

## 4. 타이머 해상도

### 현재 상태

이 프로젝트는 타이머 해상도를 조정하지 않고 Windows 기본값(약 15.6ms)을 그대로 사용한다.

- **Zone Tick** — `lastTick += GAME_TICK`으로 누적 보정하므로 드리프트가 쌓이지 않는다. 개별 틱 간격은 흔들려도 장기 평균은 유지되며, 5,000명 부하 테스트에서 TPS 19~21로 확인했다.  
- **NetTimer** — 1ms 주기로 `steady_clock`을 캐시하려 했으나 `sleep_for(1ms)`가 다음 클럭 틱까지 자므로 실제 갱신은 약 15.6ms 간격(≈64Hz)이다. RTT는 송신·수신에서 두 번 읽으므로 오차가 최대 ~31ms까지 벌어진다. 현재 RTT 용도가 로깅과 클라이언트 표시뿐이라 이 정밀도로 충분하다고 판단했다.  

### 해상도를 올리는 두 가지 방법

| | `timeBeginPeriod(1)` | high-resolution waitable timer |
|---|---|---|
| 방식 | 주기적 클럭 인터럽트 주기 자체를 변경 (15.6ms → 1ms) | 하드웨어 타이머에 만료 시각을 직접 등록 |
| 인터럽트 발생 | 타이머 유무와 무관하게 초당 1000회 고정 | 실제 만료 시점에만 발생 |
| 인터럽트당 비용 | 스케줄러 틱 전체 (타임슬라이스 회계, 만료 스캔) | 해당 타이머 처리만 |
| 비용 기준 | **시간**에 비례 | **요청한 타이머 수**에 비례 |
| 영향 범위 | Windows 10 2004부터 프로세스 단위 (이전에는 전역) | 프로세스 내부 |

해상도 상한은 `timeBeginPeriod`가 정수 ms를 받으므로 **1ms가 최대**이고, waitable timer는 만료 시각을 **100ns 단위**로 받는다. 다만 실제로 깨어나는 정밀도는 스케줄링 지연이 지배하므로 API 단위만큼 나오지는 않는다.  

`timeBeginPeriod`는 틱 주기를 100ms로 늘려도 인터럽트가 1000Hz 그대로지만, waitable timer는 요청한 만큼만 비용을 낸다.  
또한 Windows 8부터 커널이 타이머를 만료 시각순 정렬 리스트와 coalescing으로 관리하므로 대기 스레드가 많아도 전체를 스캔하지 않는다.  

따라서 **`CREATE_WAITABLE_TIMER_HIGH_RESOLUTION`(Windows 10 1803+) 이 우선 선택지**다. 
전역 클럭 주기를 바꾸지 않으므로, 타이머를 쓰지 않는 다른 프로세스에 비용을 전가하지 않는다.  

### 언제 필요한가 — 되감기(lag compensation)

같은 MMORPG라도 전투 방식에 따라 요구 정밀도가 다르다.  

논타겟·핵앤슬래시 계열은 타격 판정이 빠르고 조밀해서, 서버가 **클라이언트가 그 화면을 봤던 시점으로 월드 상태를 되감아** 판정하는 lag compensation이 들어가는 경우가 있다.   
되감을 시점은 RTT로 계산하므로 **되감기 정확도의 상한이 곧 RTT 측정 정확도**가 된다.  

| | RTT 오차 | 되감기 판정 |
|---|---|---|
| 현재 (캐시 시계 + 기본 해상도) | ~31ms | 50ms 틱 기준으로 스냅샷을 한 틱 이상 잘못 고를 수 있음 |
| HR waitable timer 적용 | ~2ms | 틱 내부 오차로 수렴 |

즉 되감기를 도입하는 순간 타이머 해상도는 선택이 아니라 요구사항이 된다.

### 결론

현재는 되감기 판정이 없고 RTT가 게임 로직에 관여하지 않으므로 적용하지 않는다.
되감기를 도입하거나 RTT가 판정 경로에 들어가는 시점에 `NetTimer`의 대기를 `CREATE_WAITABLE_TIMER_HIGH_RESOLUTION` 기반으로 교체한다. `GetTimeMS()` 인터페이스는 그대로 두고 내부 대기만 바꾸면 되므로 호출부 변경은 없다.

## 5. 클라이언트 틱 처리
클라이언트에서도 서버 틱에 맞춰 상태 업데이트를 수행한다.
- Move와 같은 지속 입력 요청은 서버 Tick 기준으로 처리되므로, 별도의 송신 루프가 필요하다.
- 클라이언트 Tick은 프레임 단위와 일치하지 않음
	- 서버에서 수신한 Tick 기준 상태를 동기화
	- **내 캐릭터(예측)** — 입력을 로컬에서 먼저 굴려 좌표를 만들고 그 좌표를 서버로 보낸다. 서버 좌표와의 격차가 임계값을 넘을 때만 스냅한다. 매 프레임 서버 좌표로 끌어당기면 앞서가는 예측이 뒤로 밀려 러버밴딩이 되기 때문이다.
	- **원격 엔티티(스무딩)** — 마지막으로 받은 좌표를 향해 실제 이동 속도와 같은 속도로 따라가 프레임 사이를 메운다. 스냅샷 두 개를 버퍼링해 그 사이 시각을 렌더하는 보간은 적용하지 않았다. 
> 즉, 클라이언트는 서버 Tick을 기준으로 게임 상태를 맞추고, 화면 표시는 내 캐릭터의 예측과 원격 엔티티의 스무딩으로 채운다.

## 6. 참고
- [ZoneThreadSet.cpp](CoreLib/ZoneThreadSet.cpp)
- [NetTimer.h](NetLibrary/NetTimer.h)
- [PingLoop](PingLoop.md) — RTT 측정 경로
