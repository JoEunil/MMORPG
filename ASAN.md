# ASAN 테스트 분석

## 1. 개요
AddressSanitizer (ASan)는 네이티브 코드의 메모리 버그 감지를 위한 컴파일러 기반 도구이다.  

ASan은 다음을 감지한다.
- 스택 및 힙 버퍼 오버플로우/언더플로우
- 프리 후 힙 사용
- 더블 프리/와일드 프리

ASan 빌드는 오버헤드가 크기 때문에 테스트에서만 사용한다.  

```powershell
# ASan 런타임 DLL 경로
$env:PATH = "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\<버전>\bin\Hostx64\x64;$env:PATH"

# 파이프 경유 시 한글 깨짐 방지
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8

# 정상 종료 시 내부 통계를 추가로 찍는 옵션
$env:ASAN_OPTIONS = "atexit=1"

.\MainServer.exe *>&1 | Tee-Object asan_run.log
```
powershell에서 위 명령을 통해 asan 결과를 수집한다.  

> ASan 테스트 중 발견한 종료 관련 버그는 [GracefulShutdown.md](GracefulShutdown.md)에 정리해놓았다. (일부는 ASan이, 일부는 종료 로그가 잡았다.)  

## 2. ASAN exit stats 읽는 법

### malloced vs freed  
- 총 할당량/호출 수 vs 총 해제량/호출수  
- 원래 의도: MSVC ASan은 leak 검출이 없어(Leak Sanitizer 미지원) 이 두 줄의 호출 수 차이(malloced - freed)를 사실상 수동 leak 지표로 쓸 수 있다.
  - 전역/싱글턴처럼 프로세스 종료까지 의도적으로 들고 있는 할당도 포함되어 "차이 = 무조건 leak"는 아님
  - 부하 테스트를 오래 돌렸는데 차이가 부하량에 비례해서 커지면 leak 신호
- **그러나 이 빌드에서는 이 지표를 쓸 수 없다.** 모든 시나리오에서 free 호출 수가 malloc 호출 수보다 많다 — 물리적으로 해제가 할당보다 많을 수 없는데도.
  - `758,985→1,386,924` / `5,097,704→10,051,438` / `14,515,567→28,887,982` / 유닛 `120,985→241,288` (약 1.8~2.0배, 부하 클수록 2배에 수렴)
  - realloc 호출은 수천 건뿐이라 이 격차를 설명하지 못함 → 이 빌드의 ASan 카운터가 free를 과다 집계하는 것으로 판단된다.
  - 그 결과 malloced/freed 바이트 총량도 신뢰할 수 없다(부하 시나리오에선 freed가 malloced를 넘기도 함).
  - **→ 누수 판정은 size class별 `inuse`의 시간 축 추세를 주 지표로 사용한다.**
 
### realloc  
- 어떤 버퍼가 크기를 다시 잡았다 뜻, 대부분 vector 류의 성장  
- 이 숫자가 크면 reserve 없이 쓰는 vector나 어딘가 Pool을 안타는 동적 버퍼가 생겼다는 신호  
- 고정 capacity로 풀링하는 구조에서는 이 값이 0에 가까워야 정상

### red zones  
- ASan이 각 할당 앞뒤에 붙이는 오염 감지용 완충 영역
- 오버플로우가 이 영역을 건드리면 검출되는 원리
- 할당당 고정 크기라 총량은 할당 개수에 비례한다 (크기 무관). 값이 크면 할당 건수가 많다는 뜻

### freed vs really freed  
- free() 호출 시 메모리를 바로 반환하지 않고 격리 구역에 잡아두는데(use-after-free 검출을 위해), "freed"는 free 호출된 양, "really freed"는 격리 기간 지나서 실제 반환된 양. 
- 차이가 커도 버그 아님, 그냥 격리 큐에 쌓여 있는 것뿐.

### mmaped  
- ASan이 OS에서 가져간 총 주소 공간(shadow memory 포함)
- 실제 메모리 사용량 측정용으로는 부적합
- ASan 오버헤드가 섞여 있어서, 서버 메모리 프로파일링은 ASan 없는 빌드에서 따로 측정 필요.

### mallocs by size class  
- 클래스:횟수 분포. 
- 할당 패턴 파악용인데, 특정 사이즈 클래스에 호출이 비정상적으로 몰려 있으면 그 크기 객체가 풀링 없이 반복 할당되고 있다는 힌트
- 풀 도입/튜닝 대상 찾을 때 유용

### size-class 할당자 통계
- "어느 크기의 객체가 새는가"를 좁히는 2단계 도구
- ex. 128바이트 클래스의 inuse만 실행할수록 증가하면, sizeof(Packet)이나 sizeof(Session) 같은 걸 확인해서 용의자를 바로 좁힐 수 있다.


## 3. 테스트 시나리오 3종 결과

3가지 시나리오로 ASan 빌드를 테스트 했다.    
1. 서버 실행 후, 1분 뒤 종료
1. 서버 실행 후, 2000명 더미테스트 1분 후 종료
1. 서버 실행 후, 2000명 더미테스트 10분 후 종료

세 시나리오 모두 ASAN 에러 리포트 없음 (heap-use-after-free(UAF) / heap-overflow / access-violation 전부 없음).

### 전체 요약

| 지표 | 0명 / 1분 | 2000명 / 1분 | 2000명 / 10분 |
|---|---|---|---|
| malloced | 1666M | 2025M | 2641M |
| malloc calls | 758,985 | 5,097,704 | 14,515,567 |
| freed | 1431M | 2144M | 3376M |
| free calls | 1,386,924 | 10,051,438 | 28,887,982 |
| realloced | 0M / 2,294 calls | 1M / 13,997 calls | 1M / 12,297 calls |
| red zones | 73M | 185M | 406M |
| 에러 리포트 | 없음 | 없음 | 없음 |

> free calls가 malloc calls보다 많다(물리적으로 불가능 → 카운터가 free 과다 집계) → malloc/free 기반 leak 지표는 이 빌드에서 사용 불가. 누수는 아래 `inuse` 시간 축 추세로 판정한다.

### size class별 `inuse` 비교

`inuse`: 종료 시점 살아있는 메모리 영역 갯수  
→ 의도적 미해제(전역, 안 부수는 풀) + quarantine 잔재 + 메모리 누수   

> 판정 원칙: `inuse`는 quarantine에 보류된 '해제 완료' 블록까지 포함한다. 따라서 inuse 절대값이 아니라 시간 축 증가 추세로만 누수를 판정한다.     

| class (size) | 0명/1분 | 2000명/1분 | 2000명/10분 | 판정 |
|---|---|---|---|---|
| 45 (40KB) | 36636 | 36675 | 37089 | 고정 풀 — 시간 10배에도 +1% (노이즈) |
| 21 (640B) | 30192 | 30206 | 30197 | 고정 풀 |
| 23 (896B) | 15330 | 15389 | 15388 | 고정 풀 |
| 26 (1536B) | 546 | 575 | 578 | 고정 풀 |
| 03 (48B) | 7687 | 16861 | 14602 | 세션 트랜션트 (시간 무관, 감소) |
| 02 (32B) | 5955 | 9387 | 8281 | 세션 트랜션트 |
| 06 (96B) | 11659 | 14176 | 13967 | 세션 트랜션트 |
| 12 (192B) | 1157 | 3519 | 3236 | 세션 트랜션트 |
| 18 (384B) | 390 | 2475 | 2473 | 세션 트랜션트 |
| 13 (208B) | 632 | 641 | 615 | 안정 |
| 20 (512B) | 360 | 322 | 375 | 안정 |  

> *트랜션트(transient) = 수명이 짧은, 뭔가에 붙어서 생겼다 사라지는 할당.

### 분석 결론

1. 메모리 안전성: 세 시나리오 모두 UAF/overflow/AV 없음. 앞서 수정한 소멸 순서 UAF·null deref 재발 안 함.

2. 고정 풀 (접속 무관, 로드의 최대 보유분):
   - class 45, class 21, 23도 접속 0명/2000명에서 동일 → 고정 풀.
   - 이들이 상주 메모리(~1.5GB)의 대부분이며 접속 수·시간과 무관.

3. 세션 트랜션트 (초당 수백만 alloc, 낮은 inuse):
   - class 02/03/06 등은 alloc 수백만인데 `inuse`는 만 단위 이하 → 누적 없이 정상 회수.

4. 누수 판정 (핵심):
   - 2000명에서 시간을 1분 → 10분(10배) 늘려도 어떤 클래스도 시간 비례로 증가하지 않음.
   - → **시간 비례 증가 없음 = 누수 없음.**

## 4. 유닛테스트 ASAN 검증

핵심 컴포넌트 Google Test를 ASAN 빌드로 실행.   
43 tests / 9 suites 전부 통과, ASAN 에러 리포트 없음.  

### 실행 요약

| 지표 | 값 |
|---|---|
| 테스트 | 43 tests / 9 suites, 전부 통과 |
| malloc calls | 0.12M |
| 에러 리포트 | 없음 (UAF/overflow/AV) |


유닛테스트 ASAN의 목적은 실행 중 메모리 오류 부재 검증이며, 그 기준에서 통과했다.  
특히 멀티스레드 레이스 테스트(`LockFreeQueue.*Race`, `TripleBuffer.ReaderRace/WriterReaderRace`, `FixedObjectPool.MultiThreadSafe`, `NetTimer`)가 메모리 오류 없이 통과 → 동시성 자료구조 신뢰도 확보.

> `LockFreeQueue` 테스트는 할당/해제를 짝 맞춰 하는데도 종료 시 class 04(64B)가 `allocs 101,248 / frees 855 / inuse 100,393`으로 나온다.  
해제된 블록이 quarantine에 남아 `inuse`에 그대로 잡히는 것으로, inuse를 절대값이 아닌 추세로 판정해야 하는 이유의 실측 근거다.  

> gtest를 NuGet → vcpkg로 이전. (nuget 패키지는 플랫폼 도구집합이 vs2017 버전이라 빌드가 안 됨.)

## 5. 한계
- 누수 자동 탐지 불가: MSVC ASan은 LeakSanitizer 미지원이라 누수를 자동으로 리포트하지 못한다. 이 문서의 누수 판정은 `inuse` 시간 축 추세로 한 수동 판정이다. ASan이 직접 탐지한 건 UAF·오버플로우·접근 위반뿐.
- 판정이 종료 시점 1회 스냅샷: `inuse`는 종료 순간 한 번의 값이라, 더 엄밀히는 런타임 중 주기적 샘플링으로 추세를 봐야 한다.
- 지속 시간 한계: 10분까지는 안정. 수 시간 단위의 초저속 누수는 별도 장시간 런으로 확인 권장.
- 성능 측정 불가: ASan 오버헤드로 처리량이 낮으므로, 실부하(full TPS)의 메모리 성능 지표는 ASan 끈 빌드에서 따로 측정해야 한다.
- data race 미탐지: ASan은 스레드 경쟁을 못 잡는다. 검증하려면 TSAN이 필요하나 MSVC/Windows 미지원 → clang/Linux 빌드가 있어야 한다.
- free 카운트 신뢰 불가: 이 빌드는 free 호출/바이트를 과다 집계하므로 malloc/free 총량 지표는 쓸 수 없다.